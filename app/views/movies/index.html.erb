<div class="container mt-4">
  <div class="row">
    <% @movies.each do |movie| %>
      <div class="col mb-3">
        <%= render 'movie-card', movie: movie %>
      </div>
    <% end %>
  </div>
</div>
